# CVN Regeneration Workflow

## Purpose

This document is the contributor-facing entry point for regenerating and
verifying the complete CVN pipeline from the canonical source package.

It covers the implemented workflow after issues `#14`, `#15`, `#16`, `#25`, and
hotfixes `#4` through `#8`.

## Canonical Source Package

The canonical source package is:

```text
docs/CvnXML_v1.4.3_2.1_17012025/
```

Authoritative XML inputs:

- `XML/SpecificationManual.xml`
- `XML/CVNTreeModel.xml`
- `XML/ReferenceTables.xml`
- `XML/Subtype_Spa.xml`
- `XML/Entity.xml`
- `XML/Thesaurus.xml`

Authoritative XSD inputs:

- `XSD/CVN.xsd`
- `XSD/Common.xsd`
- `XSD/AuxTable.xsd`
- `XSD/ISOUtilities.xsd`
- `XSD/SpecificationManual.xsd`
- `XSD/CVNTreeModel_v1.0.xsd`
- `XSD/ReferenceTables.xsd`
- `XSD/Subtypes.xsd`
- `XSD/Entity_v1.4.xsd`
- `XSD/Thesaurus.xsd`

Do not replace these inputs with downloaded files or partial copies unless the
roadmap explicitly records a canonical package change.

## Environment Setup

Install the documented environment:

```bash
uv sync --group codegen --group testing
uv pip install -e .
```

The editable install is required because the repository uses a `src/` layout.

## Complete Workflow

Run structural generation for all core and auxiliary targets:

```bash
uv run python -m cvn_codegen.xsdata_runner all
```

This regenerates structural bindings under `src/generated/` for:

- `generated.cvn`
- `generated.specification_manual`
- `generated.tree_model`
- `generated.reference_tables`
- `generated.subtypes`
- `generated.entity`
- `generated.thesaurus`

Run canonical domain generation:

```bash
uv run python -m cvn_codegen.domain_model_generator
```

This regenerates domain-oriented output under `src/models/cvn/generated/`. The
current canonical generator emits `105` files when source inputs and generator
policy remain unchanged.

Run canonical conceptual diagram generation:

```bash
uv run python -m cvn_codegen.conceptual_model_diagrams --output-dir docs/diagrams
```

This regenerates PlantUML source diagrams under `docs/diagrams/` from the issue
`#43` `ConceptualModelInventory`. The generator emits both readable presentation
views and fuller reference views. The `.puml` files are the canonical diagram
artifacts; rendered images are optional and are not required for the repository
workflow.

Run canonical JSON Schema generation:

```bash
uv run python -m cvn_codegen.json_schema_generator
```

This regenerates the issue `#45` JSON Schema artifact at
`schemas/open_cvn.schema.json` from the same conceptual inventory. The schema
declares JSON Schema Draft 2020-12 and preserves CVN trace through
`x-open-cvn-*` extensions. Its root shape remains provisional until issue `#46`
defines the canonical Open CVN JSON representation.

Reference diagrams for large areas may be index files that point to split
`..._part_XX_reference.puml` chunks. Detailed reference chunks keep controlled
vocabulary membership in local `controlled references` notes beside each entity,
instead of using long global vocabulary dependency edges that can be hard to read
or render into clipped PNGs.

For local visual review only, render PNGs with:

```bash
plantuml -tpng docs/diagrams/*.puml
```

Run full verification:

```bash
uv run pytest -n auto tests
```

This is the default local verification command and the command executed by the
pull-request CI workflow.

## Workflow Stages

The implemented pipeline stages are:

1. core structural binding generation from `CVN.xsd`, `SpecificationManual.xsd`,
   and `CVNTreeModel_v1.0.xsd`
2. auxiliary structural binding generation from `ReferenceTables.xsd`,
   `Subtypes.xsd`, `Entity_v1.4.xsd`, and `Thesaurus.xsd`
3. normalization of `SpecificationManual.xml` and `CVNTreeModel.xml`
4. auxiliary-source loading and deterministic reference-resolution enrichment
5. XSD-enriched structural type evidence for wrapper handoff
6. semantic policy application over enriched normalized metadata
7. domain model generation from `SemanticPolicyBundle`
8. conceptual model extraction from normalized, semantic, and domain-generation
   evidence
9. conceptual PlantUML diagram generation from `ConceptualModelInventory`
10. JSON Schema generation from the conceptual inventory
11. public parser and validator workflow verification for PDF, XML, and Open CVN
    JSON inputs
12. repository verification through tests and CI

The semantic handoff is:

```text
normalized metadata + auxiliary reference resolution + structural type evidence
-> SemanticPolicyBundle
-> domain generator
-> domain-oriented Pydantic artifacts
```

`SemanticPolicyBundle` is the source of truth for generator semantics. Raw XML,
raw XSD, and generated structural bindings may support parsing, normalization,
validation, and traceability, but generator code must not use them to redefine
semantic policy.

## Repository Boundaries

- `src/generated/` is structural fidelity output generated by xsdata. Do not edit
  it manually.
- `src/cvn_codegen/` contains hand-maintained loading, normalization,
  resolution, semantic-policy, and generation logic.
- `src/models/cvn/components.py` contains hand-maintained shared domain
  components.
- `src/models/cvn/generated/` contains generated domain output. Change it through
  `uv run python -m cvn_codegen.domain_model_generator`, not manual edits.

## Controlled Reference Source Order

Normalization resolves controlled references before semantic policy consumes
them. The effective source-of-truth order is:

1. explicit side-package references such as `ENTITY@Entity.xsd` and
   `THESAURUS@thesaurus.xsd`
2. direct `ReferenceTables.xml` matches where applicable
3. subtype-backed classification through `Subtype@Subtypes.xsd`
4. hierarchical thematic classification where technical metadata supports it,
   such as `UNESCO_CODES`
5. unresolved documented exceptions and under-traced tables

Representative cases:

- `CVN_SEX_A`: direct compact table, strict-enum eligible from dynamic evidence
- `CVN_ENTITY_TYPE`: direct compact table, strict-enum ineligible because of
  delegate/open evidence
- `CVN_KNOW_A`: subtype-backed reference family
- `ENTITY@Entity.xsd`: side-package registry reference
- `THESAURUS@thesaurus.xsd`: side-package vocabulary reference
- `UNESCO_CODES`: hierarchical thematic reference
- `CVN_AGENCY_C`: unresolved manual-only reference
- `CVN_INTERVENTION_A` and `CVN_PRUEBA`: technically present but under-traced

## Semantic Policy Contract

Issue `#14` established the semantic policy contract consumed by domain
generation. Workflow documentation and generator code must treat these policy
outputs as authoritative:

- `domain_shape_kind`
- `fallback_shape_kind`
- `enum_eligibility`
- `policy_confidence`
- `wrapper_policy`
- `presence_kind`
- `cardinality_kind`
- `normalized_name`
- `naming_confidence`
- `structural_limitation_flags`
- `SemanticDecisionTrace`

The policy also defines Spanish-first domain naming, deterministic identifier
normalization, versioned override precedence, strict-enum eligibility limits,
open coded-value fallback behavior, wrapper treatment, and CVN source trace
preservation.

## Wrapper Handoff

Wrapper-aware domain generation depends on XSD-enriched normalization.

Canonical domain generation provides `CVN.xsd` and `Common.xsd` to normalization
so normalized entries can carry `StructuralTypeEvidence`. Semantic policy then
maps terminal wrapper evidence to shared components:

- `FlexibleDateValue`
- `OfficialIdValue`
- `EntityTypeValue`
- `EntityNameValue`

Custom callers that omit `CVN.xsd` and `Common.xsd` from normalization should
expect empty structural type evidence and no automatic wrapper-aware field
attachment.

## Verification Matrix

Use this command as the default verification entry point:

```bash
uv run pytest -n auto tests
```

The implemented tests cover:

- structural generation for core and auxiliary targets
- parse smoke behavior for canonical XML inputs
- the documented `CVNTreeModel.xml` XML/XSD mismatch
- normalization baseline and auxiliary-reference enrichment
- reference regressions for direct, subtype-backed, side-package, hierarchical,
  unresolved, and under-traced references
- semantic policy integration, override precedence, naming, and trace
  preservation
- wrapper handoff through structural type evidence
- domain generation, importability, determinism, ASCII output, and end-to-end
  generation
- conceptual model extraction and conceptual PlantUML diagram rendering
- JSON Schema generation, metadata, enum/open-reference treatment, determinism,
  and CLI output
- parser contract, deterministic PDF XML extraction, Open CVN JSON validation,
  trace-only CVN XML import, structured errors, and trace preservation
- source coverage for manual, tree, auxiliary, semantic, and generated artifacts

Tests that regenerate `src/generated/*` use `tests/xsdata_generation_lock.py` so
shared structural output is serialized under `pytest -n auto`.

Pull requests targeting `main` and `development` run the same full-suite command
through `.github/workflows/pr-tests.yml`. The workflow job is named `tests`.

## Known Limitations To Preserve

- `CVNTreeModel.xml` diverges from `CVNTreeModel_v1.0.xsd`; this is a documented
  source-package inconsistency.
- Structural bindings do not enforce every `xs:choice` mutual-exclusion rule.
- Generated list defaults do not reliably enforce every `minOccurs` constraint.
- Some structural attributes are generated as `object`.
- `Subtype_Spa.xml` does not provide a strict per-table bridge such as
  `CVN_KNOW_A` to subtype records.
- `CVN_AGENCY_C` remains unresolved from the package alone.
- `CVN_INTERVENTION_A` and `CVN_PRUEBA` remain technically present but
  under-traced.
- Wrapper-aware field attachment requires normalization runs that provide
  `CVN.xsd` and `Common.xsd`.

See `docs/pipeline/known_limitations.md` for the authoritative limitation
register.

## Parser Workflow

For public parser and validator usage examples, structured error handling, trace
metadata expectations, and parser-focused test commands, see:

```text
docs/development/parser_workflow.md
```
